services:
  vue-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3011:3011"  # Expose port 8000 from container to host
    volumes:
      - .:/app  # Mount the local directory into the container (for dev)
    command: ["pnpm", "run", "dev", "--port", "3011"]
